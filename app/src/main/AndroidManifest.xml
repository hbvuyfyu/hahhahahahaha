<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Storage -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Overlay window -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Services and notifications -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Network — required for auth, subscription verification, and payments -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Query installed apps -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VCam"
        android:requestLegacyExternalStorage="true"
        android:extractNativeLibs="true"
        android:usesCleartextTraffic="true">

        <!-- Auth screen (login/signup) — this is the LAUNCHER activity -->
        <activity
            android:name=".ui.AuthActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main app screen — only reachable after auth -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"/>

        <!-- Subscription screen -->
        <activity
            android:name=".ui.SubscriptionActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <!-- Account screen -->
        <activity
            android:name=".ui.AccountActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <!-- Payment WebView screen -->
        <activity
            android:name=".ui.PaymentWebViewActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <!-- Media preview as floating dialog popup -->
        <activity
            android:name=".ui.PreviewActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.VCam.Dialog"
            android:windowSoftInputMode="stateHidden"/>

        <!-- Main injection service -->
        <service
            android:name=".service.VCamService"
            android:exported="false"
            android:foregroundServiceType="camera" />

        <!-- Floating overlay window service -->
        <service
            android:name=".service.FloatWindowService"
            android:exported="false" />

    </application>

</manifest>
